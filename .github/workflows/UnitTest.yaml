name: Unit Test

on:
  workflow_dispatch:

jobs:
  Unit-Test-1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.17"

      - name: Run Tests
        run: uv run pytest